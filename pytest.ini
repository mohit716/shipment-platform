[pytest]
testpaths = tests
pythonpath = .
# The coverage gate is here rather than only in CI, so a drop is visible on
# the next local pytest rather than an hour later on a pull request.
addopts = -q --strict-markers --cov=app --cov-report=term-missing
