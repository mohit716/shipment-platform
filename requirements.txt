# Setup:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (macOS / Linux)
#   pip install -r requirements.txt
#
# Direct dependencies only, pinned. Transitive packages are resolved by pip.

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.42
aiosqlite==0.22.1
greenlet==3.5.5
