# Development and test dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
httpx==0.28.1
# anyio ships the pytest plugin that runs async tests and async fixtures.
anyio==4.15.0
pytest-cov==7.1.0

